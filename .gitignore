# Dependencies
node_modules/

# Build output & Cloudflare/Wrangler local state
dist/
.wrangler/

# Deploy-time config (may contain environment specifics; copy from config.example.js)
/config.js

# Bulky design export kept locally for provenance, not in the repo
/design/claude-design/

# Playwright run output: HTML report, traces, screenshots, and the browser cache
# (the specs themselves live in e2e/ and ARE tracked)
playwright-report/
test-results/
blob-report/
.playwright/

# Firebase CLI local cache / logs (firebase.json and .firebaserc ARE tracked)
.firebase/
firebase-debug.log
firestore-debug.log

# Python tooling
tools/__pycache__/
tools/.venv/
*.pyc

# Claude Code authoring artifacts: local tooling, not part of the app.
# (.claude/settings.json is deliberately not listed, project permissions are
# worth sharing; skills and workflows are how one person happens to work.)
.claude/skills/
.claude/workflows/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
